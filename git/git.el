;; -----------------------------------------------------------------------------
;; Git support in vc-mode
;; -----------------------------------------------------------------------------
; run "sudo apt-get install git-el" for installing the below files on debian (So they are not needed)
;(load "/usr/share/git-core/emacs/git.el")
;(load "/usr/share/git-core/emacs/git-blame.el")
;(load "/usr/share/git-core/emacs/vc-git.el")
;(add-to-list 'vc-handled-backends 'GIT)
